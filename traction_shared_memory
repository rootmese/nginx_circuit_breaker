#include "traction_shared_memory.h"

traction_shared_t *g_traction = NULL;

ngx_int_t
traction_init_shm(ngx_conf_t *cf)
{
    /*
     * aqui entra ngx_shm_zone_t
     * depois podemos implementar
     */

    return NGX_OK;
}